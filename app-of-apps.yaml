apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: app-of-apps
spec:
  destination:
    name: in-cluster
    namespace: argo-cd
  source:
    path: apps
    repoURL: 'https://github.com/alexberry/argo-cd-app-of-apps-demo.git'
    targetRevision: customise
  project: default
  # syncPolicy:
  #   automated:
  #     prune: true
  #     selfHeal: true